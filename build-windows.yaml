name: Build Windows EXE

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install pyaudio pygame numpy pyinstaller

      - name: Verify pyaudio installs correctly
        run: python -c "import pyaudio; print('pyaudio ok')"

      - name: Build EXE with spec file
        run: |
          pyinstaller BirthdayCandle.spec

      - name: Upload EXE as artifact
        uses: actions/upload-artifact@v4
        with:
          name: BirthdayCandle-Windows
          path: dist/BirthdayCandle.exe